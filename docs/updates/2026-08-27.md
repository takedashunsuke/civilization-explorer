# 2026-08-27 (続)

## 地域別観測の LLM 化

* ターン末に地域ごと LLM 観測（緊張・繁栄・不満・結束・台頭人物・軌道・因果要約）
* `last_metrics` / `region_readings` をセマンティック指標で上書き（失敗時はヒューリスティック）
* 状況パネルの地域表を件数カウントから LLM 指標へ差し替え
* 意思決定プロンプトに地域 trajectory / mood を注入

## 初期配置の点在化

* 列（サブ地域）内の初期エージェントを単一ホームではなく複数キャンプに分散
* キャンプ内は軽い放射状ジッター。近いキャンプは従来どおり集落距離でマージ
