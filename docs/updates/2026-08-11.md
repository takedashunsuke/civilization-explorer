# 2026-08-11

## やったこと

- 観測 UI を **日本語デフォルト** 化
- `@nuxtjs/i18n` で **日本語 / 英語** 切替を追加（ヘッダー）
- 制度ラベル（民主主義 / 独裁制 / 無政府）など表示文言を翻訳。API キーは英語のまま
- **自動再生**（約 0.8 秒ごとに 1 ターン）を追加
- メトリクス・初期条件（シード / 教育 / 人口）に説明文を追加
- 出来事パネル: 現在ターンを上部表示、列を主体・行動・詳細に整理、幅を拡大
- 主体 ID/表示名を `a1`, `a2`… に統一
- 人口上限を 20 → **100** に引き上げ（関係計算が重いため 1000 までは未対応）
- 日本語の禁則（単語途中改行）を抑制
- 初期条件に **BC / AD の開始年** を追加。日本・世界の時代目安を即表示（1ターン＝1年）
- 日本は **大化〜令和の元号** を収録（南北朝は南朝系）。画面左右余白を詰め、サイドバーを拡幅

## 決めたこと

- UI 文言の正本は `frontend/i18n/locales/{ja,en}.json`
- 既定ロケールは `ja`（cookie: `civ_locale`）
- 人口上限は当面 100（O(n²) の関係構築のため）
- 開始年は天文年（AD 1=1, BC 1=0）で `world.start_year` に保存。現在年 = start_year + turn
- 日本元号データは `frontend/app/utils/japanNengo.ts`（`scripts/parse_eras.py` で再生成可）

## 次やること

- Event 詳細文（backend の英語ログ）を構造化して翻訳対象にする
- 大規模人口向けに関係表現の軽量化を検討
